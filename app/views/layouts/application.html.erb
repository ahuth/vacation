<!DOCTYPE html>
<html>
<head>
  <title>Vacation</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" if user_signed_in? %>
  <%= csrf_meta_tags %>
</head>
<body <%= "ng-app=app" if user_signed_in? %>>

  <header>
    <%= render "layouts/header" %>
  </header>

  <main <%= "ng-view" if user_signed_in? %>>
    <%= yield %>
  </main>

</body>
</html>
