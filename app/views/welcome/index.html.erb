<main <%= "ng-view" if user_signed_in? %>>

  <% if user_signed_in? %>

    <script>
      var App = App || {};
      App.data = <%= @preload.html_safe %>;
    </script>

  <% else %>

    <h1>Welcome</h1>

  <% end %>

</main>
